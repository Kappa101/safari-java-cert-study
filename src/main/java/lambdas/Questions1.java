package lambdas;

import java.util.Comparator;
import java.util.function.*;

public class Questions1 {
//  Predicate<String> ps = s -> s.length();
//Predicate<String> ps = (s) -> { s.length() };
//  Supplier<String> ss = () -> ""; // GOOD :)
//Function<String, Integer> len = (s, i) -> s.length() > i;
//ToLongFunction<String> len =  (String s) -> {
//  long sum = 0;
//  for (int i = 0; i < s.length(); i++) {
//    sum += s.charAt(i);
//  }
//  return sum;
//};

//Comparator<String> cs = s1, s2 -> s2.compareTo(s1);
//Comparator<String> cs = (s1, s2) -> s2.compareTo(s1);
//Comparator<String> cs = (var s1, String s2) -> s2.compareTo(s1);
//Comparator<String> cs = (var s1, s2) -> s2.compareTo(s1);
//Comparator<String> cs = (String s1, String s2) -> s2.compareTo(s1);

//BiFunction<String, String, Integer> f = (s, t) -> s.length() - t.length();
//BiPredicate<String> f = (s, t) -> s.length() - t.length();
//Comparator<String> f = (s, t) -> s.length() - t.length();
//Supplier<String> f =  (s, t) -> s.length() - t.length();
//BinaryOperator<String> f = (s, t) -> s.length() - t.length();
}
